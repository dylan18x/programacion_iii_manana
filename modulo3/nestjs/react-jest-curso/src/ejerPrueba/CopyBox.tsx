interface Props{
    input: string
    setInput: (valu:string)=>void;
    estado: boolean
    setEstado: (valu:boolean)=>void;
}

export default function CopyBox({input,setInput,estado,setEstado}:Props){

    
}